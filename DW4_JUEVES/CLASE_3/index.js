const metodosOp = require('./calcular');

const sumar = metodosOp.suma(10,10);

console.log(`
 Suma de 10 + 10 = ${sumar}
`);


/* const COWSAY = require('cowsay'); importar

uso a los metodos 
const objVaca = {
    text: "Modulos desde node",
    e : "0o",
    T : "UU",
}
const final = COWSAY.say(objVaca)
console.log(final);
 */


